# Server Configuration
PORT=3000

# Ollama Configuration
OLLAMA_HOST=http://localhost:11434

# Additional allowed Ollama hosts (comma-separated, optional)
# Only localhost, 127.0.0.1, and private network IPs (10.x.x.x, 172.16-31.x.x, 192.168.x.x) are allowed by default
# Add additional hosts here if needed (SSRF protection)
ALLOWED_OLLAMA_HOSTS=

# Claude API (optional - leave empty if not using)
# Get your API key from: https://console.anthropic.com/
CLAUDE_API_KEY=

# Grok API (optional - leave empty if not using)
# Get your API key from: https://console.x.ai/
GROK_API_KEY=

# Voice Services (optional)
# Kokoro TTS - Text-to-Speech
TTS_HOST=http://localhost:5050

# Whisper STT - Speech-to-Text
STT_HOST=http://localhost:5051

# SearXNG - Web Search (optional)
SEARXNG_HOST=http://192.168.4.97:8888
